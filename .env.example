# Port required by the dashboard Vite dev/build configuration.
PORT=

# Base path required by the dashboard Vite configuration. Use / for the published root app.
BASE_PATH=/

# PostgreSQL connection string required only for the optional API/database workspace packages.
DATABASE_URL=